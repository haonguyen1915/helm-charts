apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "passbolt-library.fullname" . }}-test-connection"
  labels:
    {{- include "passbolt-library.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "passbolt-library.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
